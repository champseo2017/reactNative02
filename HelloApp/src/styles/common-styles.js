import {
  StyleSheet
} from 'react-native'

const commonStyles = StyleSheet.create({
  container: {
    padding: 20
  },
  textTitle: {
    color: 'blue',
    fontSize: 20,
    fontWeight: 'bold',
    marginTop: 15,
  },
  textPlain: {
    fontSize: 16
  }
})

export default commonStyles